# CMS Knitto - Login & Navigation Memory

## Base URL
- http://192.168.20.27:5420/
- Title: CMS Knitto

## Login Page
- Username input: `placeholder="Username"` (role=input, inputType=text)
- Password input: `placeholder="Password"` (role=input, inputType=password)
- Submit button: `role=button, name="LOGIN"`
- Password visibility toggle: Icon button near password field (e5/e6)
- Footer link: KN UI 2828

## Successful Login Flow
- Credentials tested: username `fikri`, password `11221122`
- After login redirects to: `/content-manager`
- Post-login indicators:
  - Header shows `role=button, name="fikri"` (username)
  - `role=button, name="Log out"` visible
  - Main content: "+ Tambah Banner Baru" button

## Sidebar Navigation (off-screen when collapsed)
- Hamburger/menu icon: `role=button, name="Icon button"` at top-left (ref e25, bbox ~14,14)
- Sidebar search: `placeholder="Cari menu"`
- Menu items: Banner, Belanja, Kelompok Kain, Nama Kain, Warna Kain, Halaman Koleksi, Detail Koleksi, E-Catalog & Pricelist, Cabang, Rekomendasi Kain

## Locator Strategy
- Prefer placeholder for login fields
- Prefer role+name for buttons (LOGIN, Log out, fikri)
- Avoid data-testid (not present)

## Notes
- URL in test doc had typo `hhttp://` — use `http://`
- No visible error messages on failed login observed during positive test

## TC01 Login Test Run (2026-06-25)

### Elemen Login yang Teridentifikasi
| Elemen | Locator Stabil |
|--------|----------------|
| Username | `placeholder="Username"` |
| Password | `placeholder="Password"` |
| Tombol Login | `role=button, name="LOGIN"` |
| Toggle visibilitas password | `role=button, name="Icon button"` (bbox dekat field password) |
| Link footer | `role=link, name="KN UI 2828"` |

### Hasil Test Positif
- Kredensial: fikri / 11221122 → BERHASIL
- Redirect ke `/content-manager`
- Verifikasi: `role=button, name="fikri"` dan `role=button, name="Log out"` terlihat
- Konten utama: `role=button, name="+ Tambah Banner Baru"`

### Hasil Test Negatif (Observasi)
- Field kosong + klik LOGIN: tetap di halaman login, tidak ada pesan error di snapshot
- Password salah: tetap di halaman login, kemungkinan toast/notifikasi (tombol close kecil muncul di pojok kanan bawah)
- Tidak ada pesan validasi HTML yang terdeteksi di accessibility snapshot

### Screenshot Evidence
- TC01-halaman-login.png
- TC01-login-berhasil.png
- TC01-login-gagal-password-salah.png
