# CMS Knitto (192.168.20.27:5420)

## Base URL
- http://192.168.20.27:5420/
- Title: "CMS Knitto"
- Root URL redirects/shows login page directly (no separate /login path needed)

## Login Page
- Navy blue full-page background
- Centered white card with "CMS Knitto" heading
- Form fields:
  - Username (textbox, placeholder "Username")
  - Password (textbox, placeholder "Password")
- Submit button: role=button, name="LOGIN" (blue button)
- No hamburger/header menu on login page

## Notes
- SPA loads with networkidle2; page ready quickly
- automation_get_page_snapshot failed with "__name is not defined" error on 2025-06-19 — use role+name locators or screenshot as fallback
- Screenshot saved as login-page.png

## Navigasi Menu (flow ke halaman target)
1. **Snapshot** — cek apakah ada **icon menu hamburger** di **pojok kanan atas** (role=button, div+svg, name "Menu" / "Icon button").
2. **Klik icon hamburger** — `automation_click` dengan `clickCenter:true` jika icon kecil; `automation_wait_for` sampai daftar menu muncul.
3. **Snapshot ulang** — baca menuitem/link yang terbuka.
4. **Cari menu target** — pilih item yang **namanya sama** atau **mendekati** permintaan user (partial match, kata kunci).
5. **Klik menu tersebut** — `automation_click` pada menuitem/link (locator `ref` dari snapshot, atau `role`+`name` / `text`).
6. **Tunggu & cek navigasi** — `automation_wait_for` type=network_idle; snapshot/cek URL atau konten halaman baru sudah muncul.
7. **Retry jika belum navigate** (ulangi sampai **3 kali** per menu):
   - Attempt 1: `automation_click` menu item
   - Jika halaman belum berubah → Attempt 2: `automation_click` **parent** menu item (wrapper/li/div pembungkus dari snapshot atau elemen induk terdekat)
   - Jika masih belum navigate → Attempt 3: `automation_click_at` pada pusat bbox menu (x,y dari snapshot)
   - Setelah setiap attempt: `automation_wait_for` + snapshot untuk verifikasi navigasi; stop jika sudah berhasil
8. **Snapshot lagi** sebelum interaksi berikutnya.

Contoh: user minta "Content Manager" → klik item yang text/name mengandung "Content Manager".

## Sidebar menu items (div Tailwind)
- Item menu CMS sering berupa `div` dengan `cursor-pointer` + teks (mis. "Rekomendasi Kain & Hasil 1Kg"), tanpa `role`/`tabindex`.
- Snapshot menangkap sebagai `role=menuitem`, `tag=div`, dengan `ref` + `bbox`.
- Klik via: `automation_click` `{ ref: "e..." }` atau `{ text: "Rekomendasi Kain" }` atau `{ role: "menuitem", name: "Rekomendasi Kain" }`.

## Post-Login Dashboard (2025-06-19)
- Successful login with fikri/11221122 redirects to `/content-manager`
- Header shows user button (name="fikri") and "Log out" button
- Main page: Content Manager with banner list and "+ Tambah Banner Baru" button
- Sidebar hamburger: Icon button di header (pernah ref e23 bbox ~14,14 — verifikasi posisi via snapshot; jika ada hamburger **pojok kanan atas**, klik di sana)
- Login locators: Username ref e1, Password ref e2, LOGIN ref e3
- automation_get_page_snapshot works correctly on both login and dashboard pages

## Session 2025-06-19 (login screenshot)
- Navigated to http://192.168.20.27:5420 — title "CMS Knitto", login page loads directly
- Snapshot refs: Username e1, Password e2, LOGIN e3 (all inViewport)
- Password field has show/hide icon buttons (e4, e5)
- automation_assert_text "CMS Knitto" failed (heading may not be in body text); form fields and LOGIN button verified visible
- Screenshot captured: login-page.png

## Session 2025-06-19 (login + screenshot)
- Login successful with fikri/11221122
- Redirected to /content-manager
- Verified: user button "fikri" and "Log out" visible
- Dashboard shows Content Manager with banner list and "+ Tambah Banner Baru"
- Screenshot: cms-login-result.png

## Session 2025-06-19 (login verification)
- Login successful with fikri/11221122
- Redirected to /content-manager after clicking LOGIN (ref e3)
- Verified dashboard: user button "fikri" (e1), "Log out" (e2), "+ Tambah Banner Baru" (e4) all visible
- No blocking popups or error messages
- Login locators confirmed: Username e1, Password e2, LOGIN e3
- Screenshot: cms-login-result.png

## Session 2025-06-23 (banner item 3 update)
- Login: fikri/11221122 → /content-manager
- Banner list: 4 items; row 3 at y~519 (edit button ref e19 in edit modal context, e18 in list after save)
- Edit banner 3: click middle action button (edit/pencil) on third row
- Edit modal title shows "3"; fields:
  - Warna Text Banner: radio e28 (Putih/white, first option), e29 (second option)
  - ID text: placeholder "Input Text Banner Bahasa Indonesia" (ref e30)
  - EN text: placeholder "Input Text Banner Bahasa Inggris" (ref e31)
  - Save: Simpan ref e18; Cancel: Batal ref e17
- Updated banner 3: color=Putih (e28), ID="ini adalah automation ID", EN="ini adalah automation EN"
- Save successful; list row 3 shows "ini adalah automation ID"
- Screenshots: content-manager-dashboard.png, banner-save-result.png, banner-form-filled-verify.png
